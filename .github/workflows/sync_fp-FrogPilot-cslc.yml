name: Sync fp-FrogPilot-cslc

on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fp-FrogPilot-cslc
          persist-credentials: false
      - name: Sync with Remote Repo
        uses: repo-sync/github-sync@v2.3.0
        with:
          source_repo: "https://github.com/garrettpall/openpilot.git"
          source_branch: "frogpilot-cslc"
          destination_branch: "fp-FrogPilot-cslc"
          github_token: ${{ secrets.PAT }}
